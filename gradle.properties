android.useAndroidX=true
android.nonTransitiveRClass=true
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Deployment URLs baked into BuildConfig (override with -P or in ~/.gradle/gradle.properties).
# Leave empty until the endpoints exist: feedback is then kept in an on-device outbox.
strudel.feedbackUrl=
strudel.sourceUrl=https://github.com/okan-sourcerer/beatblox
strudel.downloadUrl=
